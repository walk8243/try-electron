import { grey } from '@mui/material/colors';

export const open = '#00631D';

export const closed = '#AB0E17';

export const merged = '#6934BA';

export const draft = grey[700];

export const githubColor = {
	open,
	closed,
	merged,
	draft,
};

export default githubColor;
